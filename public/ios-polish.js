(() => {
 const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
 const profileKey='imessagehub_profile_details';
 const getSaved=()=>{try{return JSON.parse(localStorage.getItem(profileKey)||'{}')}catch{return {}}};
 function applePreview(){const sender=$('#campaignPreviewSender'); if(!sender||$('.ios-preview-apple'))return; const logo=document.createElement('div');logo.className='ios-preview-apple';logo.textContent='';sender.parentElement?.insertBefore(logo,sender);}
 function ensureReports(){if($('#viewReports'))return;const area=$('.platform-content-area');if(!area)return;const section=document.createElement('section');section.id='viewReports';section.className='view-panel hidden';section.innerHTML=`<div class="breadcrumb-bar">Workspace / Reports</div><div class="ios-report-view"><div class="ios-report-note"><span class="eyebrow">ANALYTICS</span><h2>Campaign Reports</h2><p>Live summary based on your current outbox activity.</p></div><div class="ios-report-grid"><article class="ios-report-card"><span>Total Campaigns</span><strong id="reportTotal">0</strong></article><article class="ios-report-card"><span>Sent</span><strong id="reportSent">0</strong></article><article class="ios-report-card"><span>Delivered</span><strong id="reportDelivered">0</strong></article><article class="ios-report-card"><span>Failed</span><strong id="reportFailed">0</strong></article></div><div class="portal-card"><div class="portal-card-header"><div><h2>Delivery Overview</h2><p>Use Outbox for individual campaign records and filters.</p></div><button class="btn-primary" id="reportOpenOutbox" type="button">Open Outbox</button></div></div></div>`;area.appendChild(section);$('#reportOpenOutbox')?.addEventListener('click',()=>document.querySelector('[data-view="viewOutbox"]')?.click());syncReports();}
 function syncReports(){const map=[['#reportTotal','#outboxTotalCount'],['#reportSent','#outboxSentCount'],['#reportDelivered','#outboxDeliveredCount'],['#reportFailed','#outboxFailedCount']];map.forEach(([a,b])=>{if($(a))$(a).textContent=$(b)?.textContent?.trim()||'0'});}
 function openReports(){ensureReports();$$('.view-panel').forEach(p=>p.classList.add('hidden'));$('#viewReports')?.classList.remove('hidden');$$('.ios-bottom-nav button').forEach(b=>b.classList.toggle('active',b.dataset.bottomAction==='reports'));syncReports();window.scrollTo({top:0,behavior:'smooth'});}
 function upgradeReports(){document.addEventListener('click',e=>{const b=e.target.closest('[data-bottom-action="reports"],.ios-quick-card[data-action="reports"],[data-ios-action="reports"]');if(!b)return;e.preventDefault();e.stopImmediatePropagation();openReports();},true);}
 function upgradeProfile(){const modal=$('#accountModal'), card=modal?.querySelector('.modal-card');if(!card||card.dataset.iosUpgraded)return;card.dataset.iosUpgraded='1';const saved=getSaved();const name=$('#modalUserName')?.textContent?.trim()||$('#welcomeName')?.textContent?.trim()||'Account User';const email=$('#modalUserEmail')?.textContent?.trim()||$('#welcomeEmail')?.textContent?.trim()||'';const id=$('#dashWelcomeId')?.textContent?.trim()||'';const initial=(name.match(/[A-Za-z]/)?.[0]||'U').toUpperCase();card.innerHTML=`<div class="ios-profile-shell"><button class="modal-close ios-profile-close" id="iosProfileX" type="button">×</button><div class="ios-profile-cover"><span class="eyebrow">ACCOUNT</span><h2>Account Profile</h2><p>Manage your contact and account information.</p></div><div class="ios-profile-identity"><div class="ios-profile-avatar">${initial}</div><div><div class="ios-profile-name">${name}</div><div class="ios-profile-email">${email}</div><span class="ios-profile-id">ID ${id}</span></div></div><div class="ios-profile-form"><div class="ios-field"><label>Full name</label><input id="iosProfileName" value="${saved.name||name}" maxlength="80"></div><div class="ios-field"><label>Mobile number</label><input id="iosProfilePhone" value="${saved.phone||''}" inputmode="tel" placeholder="+1 555 000 0000" maxlength="25"></div><div class="ios-field full"><label>Address</label><textarea id="iosProfileAddress" placeholder="Street, city, state, ZIP">${saved.address||''}</textarea></div><div class="ios-field"><label>Company / Business</label><input id="iosProfileCompany" value="${saved.company||''}" placeholder="Optional" maxlength="80"></div><div class="ios-field"><label>Country</label><input id="iosProfileCountry" value="${saved.country||''}" placeholder="Country" maxlength="50"></div></div><div class="ios-profile-actions"><button class="ios-save-profile" id="iosSaveProfile" type="button">Save Profile</button><button class="ios-reset-profile" id="iosResetPassword" type="button">Reset Password</button></div></div>`;
 const close=()=>modal.classList.add('hidden');$('#iosProfileX')?.addEventListener('click',close);$('#iosResetPassword')?.addEventListener('click',()=>document.querySelector('#btnProfilePasswordReset')?.click());$('#iosSaveProfile')?.addEventListener('click',()=>{const data={name:$('#iosProfileName')?.value.trim()||name,phone:$('#iosProfilePhone')?.value.trim()||'',address:$('#iosProfileAddress')?.value.trim()||'',company:$('#iosProfileCompany')?.value.trim()||'',country:$('#iosProfileCountry')?.value.trim()||''};localStorage.setItem(profileKey,JSON.stringify(data));const wn=$('#welcomeName');if(wn&&data.name)wn.textContent=data.name;const hn=$('#headerName');if(hn&&data.name)hn.textContent=data.name;close();});}
 function init(){applePreview();ensureReports();upgradeReports();upgradeProfile();const observer=new MutationObserver(syncReports);['#outboxTotalCount','#outboxSentCount','#outboxDeliveredCount','#outboxFailedCount'].forEach(s=>{const e=$(s);if(e)observer.observe(e,{childList:true,subtree:true,characterData:true})});}
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
